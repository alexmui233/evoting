<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Happy Voting</title>
  <!--CSS -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
  <link href="../css/myjoinedevent.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css">
  <!-- CSS only -->

  <!-- JS -->
  <script src="js/events.js"></script>

</head>

<body>
  <%- include header %>
    <div class="apdiv">
      <h1>Your joined vote event are here</h1>
      <h2>You can see what vote event you joined are listed below</h2>
      <br>
    </div>
    <% for (let event of events) { %>
    <div class="apbox">
      <div class="apboxcon">
        <div class="apboxdiv">
          <div style="width: max-content; height: max-content; float: right;">
            <% if (event.state == "result") { %>
              <a class="apbtn" href="/myjoinedevent/result/<%= event.eid %>">result</a>
            <% } else { %>
              <a class="apdisbtn">result</a>
            <% } %>
            <% if (event.state == "voting") { %>
            <a class="apbtn" href="/myjoinedevent/<%= event.eid %>">vote</a>
            <% } else { %>
              <a class="apdisbtn">vote</a>
            <% } %>
          </div>
          <div style="width: max-content;">
            <div>question: <%= event.question %><br>
              owner: <%= event.owner %><br>
              state: <%= event.state %>
            </div>
          </div>
        </div>
      </div>
    </div>
    </div>
    <% } %>
    <script src="../js/metamask.js"></script>
</body>

</html>